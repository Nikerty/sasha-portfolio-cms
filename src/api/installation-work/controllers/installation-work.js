'use strict';

/**
 * installation-work controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::installation-work.installation-work');
